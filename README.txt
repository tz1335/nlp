There are two file in the folder, FileInput is the training set + test set, and FileInput2 is the stop word set.

Can either run it in eclipse or using javac in terminal. The only thing that needs to do is to enter the number of training pieces after starting the program. The output would give the prediction of each piece.

